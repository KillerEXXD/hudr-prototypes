import { Card } from '@/components/ui'

export default function HandReplayPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-lg font-bold text-text-primary">Hand Replay</h1>
      <Card className="py-12 text-center">
        <p className="text-text-muted text-sm">Hand replay viewer coming soon</p>
        <p className="text-text-muted text-xs mt-1">Select a hand from a tournament to replay it</p>
      </Card>
    </div>
  )
}
